"""
partut — магазин ParTuT: Telegram-бот и мини-приложение к нему.

Устройство пакета — по слоям, снизу вверх. Стрелка всегда одна: верхний слой
знает про нижний, обратно — никогда. Граф импортов обязан оставаться деревом,
и это сторожит tests/test_architecture.py.

    config  cache  inputs        основание: ни от кого не зависят
       ↓
    errors  integrations/        почта наружу: Телеграм, внешнее хранилище
       ↓
    db/                          база: ядро в __init__, куски рядом файлами
       ↓
    notifications                общее для бота и сайта: письма продавцу
       ↓
    web/                         сайт: фабрика, права, ручки по темам
    bot/                         чат: команды и ночные дела
       ↓
    run.py (в корне)             единственная точка входа

Внутри пакета импортируем по полному имени (`from partut.web import auth`), а
не относительно: так видно слой, к которому идёшь, и перенос файла между
папками не превращается в правку точек.
"""
